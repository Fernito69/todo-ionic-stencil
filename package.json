{
  "name": "todo-stencil-ionic",
  "private": true,
  "version": "0.0.1",
  "description": "todo-stencil-ionic",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@ionic/core": "^5.3.1",
    "@stencil/core": "^1.17.3"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "stencil": "0.0.5"
  }
}
